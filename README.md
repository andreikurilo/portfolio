# akurilo-site

Small personal landing page for akurilo.com built with React, Vite, and TypeScript.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Notes

- Update social links in `src/App.tsx`
- Replace `hello@akurilo.com` if needed
- Deploy `dist/` to GitHub Pages
